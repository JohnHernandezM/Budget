Function GetShapeByName(ws As Worksheet, shpName As String) As Shape
    Dim shp As Shape, n As String
    n = LCase$(shpName)
    For Each shp In ws.Shapes
        If LCase$(shp.Name) = n Then
            Set GetShapeByName = shp
            Exit Function
        End If
    Next
End Function
